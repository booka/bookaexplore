<% content_for :header do   %>
  <%= include_javascripts :archives %>
  <script type="text/javascript">
    jQuery(function($) {
      $("#booka").booka({token : '<%= form_authenticity_token %>'});
      //      $.archives.init('<%= form_authenticity_token %>', '<%= documents_path(:format => :js) %>');
    });
  </script>
<% end %>